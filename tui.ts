/**
 * Terminal plugin entry. OpenCode resolves `<directory>/tui` when a plugin is
 * configured by path, and `<package>/tui` when it is installed by name.
 */

export { default } from './src/opencode/tui.js';
